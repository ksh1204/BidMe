<div class='slide_down'>
  <h3 class='float-l'>Editing Category: <%=@item_category.name%></h3>
    <%=link_to_remote 'Cancel', :url => {:controller => 'item_categories', :action => 'edit', :id => @item_category.id, :cancel => 'true'}, :html => {:class => 'float-r'}%>
    <br class='clear'>
  <%if @item_category%>      
      <%form_remote_for @item_category, :controller => 'item_categories', :action => 'update' do |f|%>
        <%=f.error_messages%>
        <%=f.label :name%>
        <%=f.text_field :name%><br />
        <hr>
        <%=f.submit 'Update'%>
        <%=link_to_remote 'close', :url => {:controller => 'item_categories', :action => 'edit', :id => @item_category.id, :cancel => 'true'}%>
      <%end%>
  <%end%>

</div>